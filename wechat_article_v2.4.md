# Trae Multi-Agent Skill v2.4：当 AI 学会"三思而后行"

不知道你们有没有这种感觉——AI 写代码，有时候真的让人又爱又恨。

爱的是速度快，半小时能干完一天的活。恨的是，你让它改个小功能，它能顺手给你重构三个文件；你让它做个登录页面，它给你整了一套设计模式全家桶。

这不是 AI 能力不行，是**行为没约束**。

v2.4 要解决的就是这个问题。

---

## 为什么从"功能堆砌"变成"行为约束"？

之前我们做了很多功能：代码地图、多角色协作、可视化任务……

但用着用着发现，光有功能不够。AI 还是那个问题——假设太多、动手太快、改动太泛、不知道啥时候算干完。

这就像一个能力很强的团队，但没有工作流程，结果要么返工，要么做了一堆没人用的功能。

v2.4 干脆不堆功能了，我们给 AI 装上一套"行为准则"。

---

## Karpathy 四大原则：AI 团队的宪法

业界大佬 Andrej Karpathy 提过四个编程原则，我们觉得说得太对了，直接拿过来当 v2.4 的核心：

### 三思而后行

写代码之前，先把假设摆出来。

比如产品说"加个用户管理"，你得先问清楚：什么叫"用户"？管理员算不算？要不要权限分级？能用手机号登录吗？

这些如果没确认就开始写，十有八九要返工。

AI 也一样。在动手之前，它应该先明确告诉用户：我理解的需求是这样的，你看对不对？或者我有几个方案，各有什么优缺点，你选哪个？

v2.4 里，每个角色现在都会先问清楚再动手。架构师问清楚技术边界，产品经理问清楚业务目标，开发者问清楚输入输出边界。

### 简单优先

这个说起来容易，做起来难。

一个简单的列表展示，非要套个 MVC 三层架构。一个查数据库的功能，非要搞个工厂模式。

Karpathy 说过一句话挺扎心的："The most important skill in programming is knowing what not to do."

v2.4 现在会检查：这段代码有没有过度抽象？有没有预留"以后可能用到"的代码？有没有为了显示技术实力而过度设计？

如果检测到这些问题，会直接标出来。

### 精准修改

改 bug 的时候，最怕的是"顺手把旁边那几行也优化了"。

本来只想修个空指针，结果不知道咋回事，线上出了个新 bug——因为改动的范围太大了。

v2.4 强调"精准手术"：只改需要的，不碰无关的。如果要改三处以上，AI 需要说明每处改动的原因。

### 目标驱动

"写完了"是个很模糊的词。

代码写完了，测试了吗？文档写了吗？边界情况考虑了吗？

v2.4 引入了验证检查点机制。每个阶段都要有明确的"完成标准"，而且要验证通过才算真正完成。

---

## 强制执行的检查器

光有原则不够，得有人盯着。

v2.4 提供了一个专门的审查能力，专门扫描代码里的违规模式。

比如：

- 代码里有 TODO/FIXME 但没处理的——违反"三思而后行"
- 有一个类只被继承了一次——可能是过度抽象
- 代码里有 mock 占位——违反"精准修改"
- 测试函数是空的——违反"目标驱动"

扫描完会生成一份报告，告诉你哪里有问题，应该怎么改。

**使用方式**：

直接用自然语言调用 skill：

```
帮我检查一下这个项目的代码，看看有没有违反 Karpathy 四大原则的地方。
重点关注：
1. 过度设计的代码（比如不必要的工厂模式、策略模式）
2. 精准修改问题（修改时有没有溢出到无关代码）
3. 目标驱动问题（有没有空测试、TODO 没处理完）
```

---

## 逻辑漏洞和业务漏洞：代码安全的第二道防线

传统代码审查主要看代码风格、设计模式、命名规范。

但有些 bug 不是"写得不规范"，而是"想得不对"。

比如：

- 浮点数比较用 `==`，结果 0.1 + 0.2 != 0.3
- 扣库存和减余额不在一个事务里，并发时钱没了库存还在
- 订单创建没关联用户ID，后来查订单找不到是谁下的

这些我们叫"逻辑漏洞"和"业务漏洞"。

v2.4 新增了一个专门的审查角色，专门找这类问题。

审查维度包括：

**逻辑漏洞**：条件判断缺陷、循环边界、状态机不一致、时序问题

**业务漏洞**：流程绕过、规则违反、数据完整性破坏、权限提升

**并发安全**：竞态条件、死锁风险、原子性缺失

审查完会生成一份详细的漏洞清单，包括风险等级和修复建议。

**使用方式**：

```
对这个项目做一次完整的代码走读。
要求：
1. 6 个角色都要参与（架构师、产品经理、独立开发者、测试专家、UI 设计师、逻辑漏洞专家）
2. 特别关注逻辑漏洞和业务漏洞
3. 输出统一代码地图和详细审查报告
```

---

## UI 设计升级：从"好看"到"好用"

之前有用户吐槽："你们这个界面确实漂亮，但用起来不太顺手。"

反思了一下，问题主要在两点：页面内联功能不够丰富，操作路径有点绕。

### 页面内联功能

所谓"内联"，就是不用跳转页面，在原地就能完成任务。

比如：

**表格行内编辑**——以前双击单元格只能复制内容，现在可以直接编辑，敲完回车就保存。要取消的话 Esc 一下就行。

**表单即时校验**——以前要等提交才知道填错了，现在输入的时候就在检查，格式不对马上提示。

**批量操作**——选中多条记录，点一下就能批量删除或者导出，不用逐个处理。

**操作反馈**——执行成功或者失败，马上有提示，不用猜到底成功没有。

### 操作流优化

另一个问题是操作路径太长。

比如改个配置要进五层菜单，找半天才找到在哪。

v2.4 优化了操作流：

- 常用功能放在更浅的层级，3 步以内能搞定
- 高频操作提供快捷键
- 复杂表单有引导步骤，不会迷路
- 操作完返回原来的位置，不丢上下文

---

## Prompt 里的原则提醒

之前的 Prompt 写完就完了，AI 执行的时候不一定记得住。

v2.4 把 Karpathy 原则融进了每个角色的 Prompt 里，每次执行都会看到提醒。

比如开发者在写代码之前，Prompt 里会提示：

"先想清楚这段代码要做什么，有没有更简单的实现方式，改动范围是不是太大了，测试能不能覆盖到。"

产品经理在分析需求的时候，会提醒：

"用户真正需要的是什么？能不能先做最小可行版本？哪些是核心需求，哪些是锦上添花？"

这些提醒不是装饰，执行的时候真的会检查。

---

## 几个真实的改进场景

说完功能，说几个具体的使用场景，感受一下 v2.4 的变化。

**场景一：让 AI 帮忙做个登录功能**

以前：AI 直接开干，半小时后给你一套 OAuth2 + JWT + Session 的完整实现。

现在：AI 先问你几个问题——需要支持哪些登录方式？要不要第三方登录？Session 存在哪？这些问题确认完了，再给你方案。

**场景二：修复一个空指针异常**

以前：AI 改了代码，顺便把几个相关的类也重构了，说是"顺便优化一下"。

现在：AI 只改空指针相关的代码，其他改动会标注出来让你确认是不是真的要改。

**场景三：代码审查**

以前：审查报告主要说命名不规范、注释太少、缩缩不一致这些问题。

现在：报告里多了一章，专门讲逻辑漏洞和并发安全风险。比如发现了库存扣减和余额扣减不在同一个事务里的问题。

---

## 怎么上手

**升级现有项目**：

直接在 Trae IDE 中安装 skill，然后重启即可。v2.4 向后兼容，之前的功能继续可用。

**使用 Karpathy 原则检查**：

```
扫描这个项目的代码，生成一份 Karpathy 原则合规性报告。
重点检查：过度设计、精准修改、目标驱动执行等方面的问题。
```

**使用多角色代码走读（含逻辑漏洞审查）**：

```
对这个项目进行多角色代码走读，6 个角色都要参与。
输出统一代码地图和审查报告，特别关注逻辑漏洞和业务漏洞。
```

---

## 写在最后

v2.4 是一个看起来变化不大，但底层逻辑很不同的版本。

我们没有加新功能——至少表面上没有。

我们做的是给已有的功能加上"约束"，让 AI 的行为更可预期、更可控。

Karpathy 说过，知道"什么不该做"比知道"怎么写代码"更重要。

v2.4，就是帮 AI 学会"什么不该做"。

---

GitHub：https://github.com/weiransoft/TraeMultiAgentSkill
文档：https://github.com/weiransoft/TraeMultiAgentSkill/blob/main/README.md

有问题可以在 GitHub 提 issue，也欢迎 star。

*版本：v2.4.0*
